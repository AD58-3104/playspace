cmake_minimum_required(VERSION 3.18)
project(Infoshare)
find_package(Protobuf REQUIRED)
protobuf_generate_cpp(PROTO_SRCS PROTO_HDRS infoshare.proto)
#protobuf_generate_cpp(PROTO_SRCS PROTO_HDRS DESCRIPTORS PROTO_DESCS pro.proto)
# add_library(infoshare SHARED ${PROTO_SRCS} ${PROTO_HDRS})
add_executable(main main.cpp ${PROTO_SRCS} ${PROTO_HDRS})
target_compile_features(main PUBLIC cxx_std_17)
target_compile_options(main PUBLIC -O2 -Wall -Wpedantic) 
target_include_directories(main PRIVATE ${CMAKE_CURRENT_BINARY_DIR})
target_include_directories(main PRIVATE ${Protobuf_INCLUDE_DIRS})
# target_link_directories(main ${CMAKE_CURRENT_BINARY_DIR})
target_link_libraries(main -lrt protobuf::libprotobuf )